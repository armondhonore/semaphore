application:
  name: semaphore
  pods:
  - name: app
    image: semaphoreui/semaphore:latest
    path: /
    servicePorts:
    - 3000
    vars:
      SEMAPHORE_DB_DIALECT: bolt
      SEMAPHORE_ADMIN: admin
      SEMAPHORE_ADMIN_PASSWORD: nexlayer2024
      SEMAPHORE_ADMIN_NAME: Admin
      SEMAPHORE_ADMIN_EMAIL: armond@nexlayer.com
      SEMAPHORE_ACCESS_KEY_ENCRYPTION: "a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3=="
    volumes:
    - name: semaphore-data
      mountPath: /var/lib/semaphore
      size: 5Gi
